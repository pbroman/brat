# The server under test holds its users in one in-memory map and every test clears it first, so tests
# must not overlap. Stated rather than left to the default, since the default is what would change.
junit.jupiter.execution.parallel.enabled=false
